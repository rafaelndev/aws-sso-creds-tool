{
  "name": "aws-sso-creds-tool",
  "description": "script to (almost)auto update aws sso credentials",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/leandrosalo/aws-sso-creds-tool.git"
  },
  "scripts": {
    "start": "node ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://github.com/leandrosalo/aws-sso-creds-tool#readme",
  "dependencies": {
    "@aws-sdk/client-sso": "^3.18.0",
    "@aws-sdk/client-sso-oidc": "^3.18.0",
    "conf": "^10.1.2",
    "configparser": "^0.3.9",
    "open": "^8.2.1"
  },
  "keywords": [
    "aws",
    "sso",
    "cloud",
    "credentials",
    "aws sso credentials",
    "aws authentication",
    "aws single sign on",
    "single sign on"
  ],
  "bin": {
    "auto-aws": "./app.js"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=16.0.0"
  }
}
